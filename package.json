{
  "name": "asia-major-eateries.github.io",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Astro marketing and live restaurant-location discovery site for Asia Major Eateries.",
  "engines": { "node": ">=22.19.0" },
  "scripts": {
    "images": "node scripts/materialize-menu-images.mjs",
    "dev": "npm run images && astro dev",
    "build": "npm run images && astro build && node scripts/verify-built-site.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run images && npm run check && npm test && astro build && node scripts/verify-built-site.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
